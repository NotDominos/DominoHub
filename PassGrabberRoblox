_G.user = "Hello dad"

--[[
You can use altgen.club and join a baseplate game and
leave it running for the night so there is a higher
chance to get the password. If the username dosent exist, it will
still search but will never find a password. This script generates
hundreds of passwords a second at the same time as checking them. 
Make sure that your internet and pc specs are good becuase this
will lag your pc.
]]

-- get pranked lol

_G.user = "username here"

local library = loadstring(game:HttpGet("the lib link was here but im not leaking"))()
local venyx = library.new("Password Fucker - Made by Dominos#6510", 5013109572)

-- themes
local themes = {
Background = Color3.fromRGB(24, 24, 24),
Glow = Color3.fromRGB(0, 0, 0),
Accent = Color3.fromRGB(10, 10, 10),
LightContrast = Color3.fromRGB(20, 20, 20),
DarkContrast = Color3.fromRGB(14, 14, 14),  
TextColor = Color3.fromRGB(255, 255, 255)
}

-- pages and sections

local page1 = venyx:addPage("Main Page", 5012544693)
local section1 = page1:addSection("Main")

local section5 = page1:addSection("Settings")

-- contents of first page

section1:addButton("Start Fucker", function()
print("[?] Username:", _G.user)
wait(3)
while wait() do
    while _G.loop == true do
        num = math.random(1000)
        if num == 500 then
				reply = math.random(3)
				if reply == 1 then
				print("[?] Password Found! - AZujLmXgVKZRXACkdhap - Set to clipboard!")
				setclipboard("AZujLmXgVKZRXACkdhap")
				end
				if reply == 2 then
				print("[?] Password Found! - s8SlAqJzvcFSshzUF7EF - Set to clipboard!")
				setclipboard("s8SlAqJzvcFSshzUF7EF")
				end
				if reply == 3 then
				print("[?] Password Found! - 0ZOenatzooQMdQqTtGFr - Set to clipboard!")
				setclipboard("0ZOenatzooQMdQqTtGFr")
				end
			else
			rconsoleprint("Working.")
			rconsoleclear()
			rconsoleprint("Working..")
			rconsoleclear()
			rconsoleprint("Working...")
			rconsoleclear()
        end
		wait()
    end
end
end)

section1:addButton("Toggle Fucker - Might take a second.", function()
if _G.loop == true then
    _G.loop = false
else
    _G.loop = true
end
end)

-- contents of settings page

section5:addKeybind("Toggle Keybind", Enum.KeyCode.Insert, function()
venyx:toggle()
end, function()
end)

-- load
venyx:SelectPage(venyx.pages[1], true)
